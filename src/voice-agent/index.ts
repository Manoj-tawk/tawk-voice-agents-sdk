/**
 * Voice Agent Module
 * 
 * Multi-Modal Voice Agent with:
 * - Audio OR Text input
 * - ALWAYS Text + Audio output
 * - agents-sdk as the LLM layer
 */

export { VoiceAgent, VoiceAgentConfig } from './voice-agent';
export { VoiceAgent as default } from './voice-agent';
